import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App initializes', (WidgetTester tester) async {
    // Placeholder test - the app requires Supabase configuration
    expect(true, isTrue);
  });
}
