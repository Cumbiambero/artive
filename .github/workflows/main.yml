name: Keep Supabase Alive
on:
  schedule:
    - cron: '0 0 */3 * *'  # Every 3 days
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - run: curl -s "${{secrets.SUPABASE_URL}}/rest/v1/" -H "apikey:${{secrets.API_KEY}}"
